{
  "name": "cartesian-editor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --format=esm --outfile=../dist/chemistry-cartesian-editor/plugin.js --minify --loader:.css=text",
    "watch": "esbuild src/plugin.ts --bundle --format=esm --outfile=../dist/chemistry-cartesian-editor/plugin.js --watch --loader:.css=text"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "~5.9.3"
  }
}
