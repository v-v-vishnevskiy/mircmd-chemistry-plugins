{
  "name": "molecular-visualizer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --format=esm --outfile=../../dist/chemistry-molecular-visualizer/plugin.js --minify --loader:.css=text",
    "watch": "esbuild src/plugin.ts --bundle --format=esm --outfile=../../dist/chemistry-molecular-visualizer/plugin.js --watch --loader:.css=text"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "~5.9.3"
  }
}
